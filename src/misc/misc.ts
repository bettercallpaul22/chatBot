import { IntentTypes } from "../model"

export const item_prices = {
    pizza: 300,
    bread: 450,
    cheese: 1050,
    burger: 200
  }

  export const intent_: IntentTypes = {
    add_order: 'add_order',
    remove_order: 'remove_order'
  }